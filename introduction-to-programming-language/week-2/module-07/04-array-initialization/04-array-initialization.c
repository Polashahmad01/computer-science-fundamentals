#include<stdio.h>
int main() {
  
  int arr[5]; // Declare an array
  int array[5] = {10, 20, 30, 40, 50}; // Declare adn initialize an array
  int arr2[] = {10, 20, 30};

  for(int i = 0; i < 3; i++) {
    printf("%d ", arr2[i]);
  }

  
  return 0;
}